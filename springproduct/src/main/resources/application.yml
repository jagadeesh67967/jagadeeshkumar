spring:
  datasource:
    url: jdbc:mysql://localhost:3306/javabatch
    password: root
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
       # dialect: org.hibernate.dialect.MySQLDialect
        
      